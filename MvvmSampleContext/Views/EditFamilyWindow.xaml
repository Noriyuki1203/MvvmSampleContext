<views:DialogWindowBase x:Class="MvvmSampleContext.Views.EditFamilyWindow"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:views="clr-namespace:MvvmSampleContext.Views"
                        Title="{Binding Title}"
                        Height="240"
                        Width="420"
                        WindowStartupLocation="CenterOwner"
                        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="名前"
                   VerticalAlignment="Center" />
        <TextBox Grid.Column="1"
                 Margin="4,0,0,8"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1"
                   Text="続柄"
                   VerticalAlignment="Center" />
        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Margin="4,0,0,8"
                 Text="{Binding Relationship, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2"
                   Text="年齢"
                   VerticalAlignment="Center" />
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 Margin="4,0,0,12"
                 Text="{Binding AgeText, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="3"
                   Grid.ColumnSpan="2"
                   Foreground="Tomato"
                   Text="{Binding ErrorMessage}"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="4"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="キャンセル"
                    Width="90"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}" />
            <Button Content="保存"
                    Width="90"
                    IsDefault="True"
                    Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</views:DialogWindowBase>
